# Release Status Matrix

This matrix prevents two mistakes:

- Treating the current public preview as a final v1.0 release.
- Treating the current Agent Workspace slices as the finished v3 MarginNote Knowledge Agent OS.

Current release candidate: `0.4.41` public preview.

Current UI shell: `Chat Mode` plus first-stage `Agent Workspace`. The runtime-required Knowledge OS anchors are:

- `knowledgeConsolePanel`
- `studioCanvasPanel`
- `operationLedgerDrawer`
- `sourceRegistryPanel`
- `verificationReportPanel`
- `externalGatewayPanel`
- `workflowBuilderBoardPanel`
- `skillCenterPanel`

These anchors are required by WebView static tests, `doctor.py`, `single_document_acceptance.py`, and `ui_functional_acceptance.py`. A deployed runtime that does not report them is stale or incomplete.

## Kernel Matrix

| Area | Implemented now | First-stage boundary | Verification | Preview / final impact |
| --- | --- | --- | --- | --- |
| Live MN Object Kernel | `object_kernel.py` builds `codex.mn.mnObject.v1` objects, persists scoped `codex.mn.mnObjectRegistry.v1`, and ingests native scan evidence. The Notebook Workspace `MN 对象` matrix axis requests `scan_mn_objects` when no native evidence exists, then switches to `open_object_browser` / `object_browser` after `native_object_scan` evidence is present. | Still a first-stage registry, not full live Finder-style notebook browsing. | `tests.test_object_kernel`, Object Browser static/runtime gates, `native_object_scan` evidence, `test_notebook_workspace_matrix_opens_object_browser_after_native_scan`. | Required for Knowledge OS claims; public preview can ship with first-stage registry if documented. |
| Knowledge Console Matrix | `knowledge_console.py` builds `codex.mn.knowledgeConsoleMatrix.v1` and seven `codex.mn.knowledgeConsoleAxis.v1` rows for source inventory, MN objects, mind-map baseline, card coverage, workflow runtime, Operation Ledger, and verification evidence. `notebook_workspace` attaches that output before Object Intake and Task Composer consume the same state. | This is a zero-message matrix and routing layer, not a complete Knowledge IDE. It summarizes conservative evidence and recommended actions, but cross-document graph editing, full coverage accounting, and automatic repair remain future work. | `tests/test_knowledge_console.py`, `notebookKnowledgeMatrix`, `notebookKnowledgeMatrixList`, `codex.mn.knowledgeConsoleMatrix.v1`, Web static gates. | Required before claiming the first screen has moved from chat-first to notebook-state-first. |
| Object Intake router | `object_intake.py` builds `codex.mn.objectIntake.v1`, treating the current document, selection, or node as the workbench input object. `notebook_workspace` attaches that output. It exposes `route_source_registry`, `route_object_browser`, `route_mindmap_studio`, `route_card_factory`, `route_workflow_builder`, `route_skill_center`, and `route_verification_center`, each backed by the same real workspace action used elsewhere. | This is a first-stage router, not a full visual task composer. It changes the first-screen interaction from prompt-first to object-first, but route internals still depend on existing first-stage panels. Keeping it in `object_intake.py` prevents the zero-message object router from being hidden in the Companion adapter layer. | `tests/test_object_intake.py`, `test_notebook_workspace_summarizes_objects_workflows_review_and_mindmap_cache`, `notebookObjectIntake`, `notebookObjectIntakeRoutes`, `codex.mn.objectIntakeRoute.v1`, Web static gates. | Required before claiming the product has visibly moved beyond chat plus answer buttons. |
| Notebook Runbook | `notebook_runbook.py` builds `codex.mn.notebookRunbook.v1`, eight `codex.mn.notebookRunbookStep.v1` rows, `codex.mn.notebookRunbookContinue.v1`, and `codex.mn.notebookRunbookAutoPlan.v1` from the same Notebook Workspace state. `companion.py` now only supplies the latest preflight run and keeps storage/ledger concerns outside the pure Runbook kernel. | This is an executable first-screen runbook, not the final autonomous study planner. It can identify and sequence safe preparation gaps, but write actions, card generation, and MarginNote mutations still require explicit workflow, Diff, confirmation, and ledger paths. | `tests/test_notebook_runbook.py`, `test_notebook_workspace_summarizes_objects_workflows_review_and_mindmap_cache`, `notebookWorkspaceRunbook`, `notebookWorkspaceRunbookContinueButton`, `codex.mn.notebookRunbook.v1`, Web static gates. | Required before claiming Notebook Workspace is a real operating checklist rather than another status panel. |
| Object Task Composer | `object_task_composer.py` builds `codex.mn.objectTaskComposer.v1`, `codex.mn.objectTaskDraft.v1`, and `codex.mn.objectTaskWorkflowCandidate.v1`, converting Object Intake routes into task drafts with route actions, evidence, expected output, optional `agent_plan` compile actions, and controlled `workflow_start` actions for `mindmap_reorganize`, `selection_to_cards`, and `paper_deep_reading`. `notebook_workspace` attaches that output, while the WebView exposes `notebookObjectTaskComposer`, `notebookObjectTaskComposerSummary`, and `notebookObjectTaskComposerList`, forwards object/prompt payload into Operation Compiler, and can start the existing Workflow Runtime from a task candidate. | This is still a draft-first composer, not the full visual Workflow Builder. It cannot yet compose arbitrary multi-object plans or migrate skill schemas, but it prevents the first screen from stopping at route buttons and keeps task composition out of the Companion adapter layer. Workflow start still keeps write steps behind confirmation points. | `tests/test_object_task_composer.py`, `test_notebook_workspace_summarizes_objects_workflows_review_and_mindmap_cache`, `codex.mn.objectTaskWorkflowCandidate.v1`, `data-object-task-id`, `refreshAgentPlan(true, payload)`, `action === 'workflow_start'`, Web static gates. | Required before claiming object-first UI has become object-level task composition. |
| Workflow Builder Board | `workflow_builder.py` builds `codex.mn.workflowBuilderBoard.v1` from workflow runtime summaries and Object Task Composer candidates. `workflow_list` and `notebook_workspace` attach that board, while the WebView exposes `workflowBuilderBoardPanel`, `workflowBuilderBoardSummary`, and `workflowBuilderBoardLanes`, splitting visible workflow work into `draft_candidates`, `active_runs`, `waiting_confirmation`, and `evidence` lanes. Task candidates can start controlled `workflow_start` runs, and run cards open the Run Inspector. | This is a first visual board on top of Workflow Runtime, not the final drag-and-drop multi-object Workflow Builder. It cannot yet author arbitrary plans or migrate skill schemas, but it makes workflow state visible beyond pending counts and keeps board construction out of the Companion adapter layer. | `tests/test_workflow_builder.py`, `test_notebook_workspace_summarizes_objects_workflows_review_and_mindmap_cache`, `codex.mn.workflowBuilderBoard.v1`, `workflowBuilderBoardPanel`, `draft_candidates`, `waiting_confirmation`, Web static/doctor/single-document gates. | Required before claiming long-running work has moved beyond queue-only behavior. |
| Mindmap Studio entry | The Notebook Workspace `脑图基线` matrix axis requests `mn_read_tree` when no current tree cache exists, then switches to `open_mindmap_studio` after a real mind-map baseline is cached. The workspace surface now focuses `mindmapStudioPanel` instead of jumping straight to the Diff workbench. | Still a first-stage Studio. It opens and summarizes cached tree/Diff/transaction state; complete visual native-tree editing remains future v2/v3 work. | `test_notebook_workspace_summarizes_objects_workflows_review_and_mindmap_cache`, Web static workspace-surface checks, `mindmapTreeReadFinished` evidence. | Required to move the first screen from status cards toward real workbench entry points. |
| Card Factory / Workflow entry | The Notebook Workspace `卡片覆盖` axis now opens `open_card_factory` / Card Factory instead of behaving like a passive review-count label. The `Workflow` axis keeps `workflow_list` while no run exists, then switches to `open_workflow_builder` once a workflow run exists. | These are still first-stage workbench entries; Card Factory coverage and visual Workflow Builder are not complete v3 systems yet. | `test_notebook_workspace_summarizes_objects_workflows_review_and_mindmap_cache`, `test_notebook_workspace_returns_zero_message_study_program_recommendations`, Web static action checks. | Required before the Knowledge Console can be treated as a zero-message workbench rather than a status board. |
| Source Registry action evidence | `source_registry.py` builds `codex.mn.sourceRegistry.v1` from current MN document, cached PDFs, explicit file paths, uploads, and file search roots. Source actions record `codex.mn.sourceRegistryActionRun.v1`. The `来源清单` matrix axis now opens `open_source_registry` when readable material exists, and only shows cache/file/path repair actions when material is missing. | Does not yet provide full cross-notebook source fabric or automatic permission repair. | `tests.test_source_registry`, `notebookWorkspaceSourceActions`, `sourceRegistryPanel`, single-document web control gate, source-axis matrix tests. | Required for reliable full-document workflows; missing readable sources should block full-document claims, not preview installation. |
| External Automation Gateway v2 | `external_gateway.py` normalizes external URL/API requests, strips secrets, rejects direct write/delete requests, and records callback lifecycle evidence. | External systems can start workflow requests; they still cannot bypass dry-run, confirmation, ledger, or rollback. | `tests.test_external_gateway`, `/external/workflow/start`, `/external/callback/success`, `/external/callback/error`, Operation Ledger external evidence. | Required before advertising external automation. Preview can expose it as local automation gateway. |
| Transactional Native Editor | AI edit transaction evidence separates note and card IDs for create/delete/failure/residual proof. Reject paths no longer count unsupported card cleanup as clean rollback. | Native object deletion/probe coverage is still incomplete; residual proof can remain `UNKNOWN` without native probe. | `tests.test_transaction_manager`, `tests.test_native_transaction_static`, Operation Ledger transaction detail, `codex.mn.residualProof.v1`. | Required for write safety claims. Final/v3 claims need stronger native probe evidence. |
| Workflow Runtime v2 | `workflow_engine.py` persists workflow runs, step state, next-step inspection, resume, retry, cancel, and event evidence. Web Run Inspector exposes steps and recovery actions. | It is a workflow state-machine slice, not a full visual Workflow Builder. | `tests.test_workflow_engine`, `workflowRunInspectorPanel`, `workflow_next_step`, `workflow_resume`, `workflow_retry_step`. | Required for replacing queue-only behavior. Preview can ship as workflow runtime slice. |
| Skill Runtime v2 | `skill_marketplace.py` validates `codex.mn.skillManifest.v1`, rejects unsafe write/delete manifests, installs valid skills, builds dry-run-first operation plans, and records `codex.mn.skillRun.v1`. | Skill packages are local and first-stage; not yet a full marketplace with upgrade/migration UI. | `tests.test_skill_marketplace`, `skillCenterPanel`, `skill_operation_plan`, `skill_run_record`, `skill_run_latest`. | Required before calling custom prompts "skills". Preview can expose local validated skills. |
| Verification Agent | `verification_agent.py` returns `codex.mn.verificationReport.v1` with strict `PASS`, `FAIL`, or `UNKNOWN` for transactions, source registries, workflow runs, and skill runs. Operation Ledger details include verification reports for workflow/transaction entries, and the WebView now exposes a first-stage object-scoped Verification Center with `codex.mn.verificationRepairPlan.v1`, recommended repair actions, and a native object-probe repair action for `native_probe_missing` transaction reports. `mnObjectExistenceProbeFinished` evidence is normalized back into transaction verification reports. | Without native object probe, transaction presence is `UNKNOWN`, not `PASS`. The current Verification Center can plan, request, and consume one class of probe, but it is not yet a full repair studio or complete native probe suite. | `tests.test_verification_agent`, `tests.test_companion_controls`, `tests.test_web_controls_static`, Operation Ledger details, `verification_report_list`, `codex.mn.verificationRepairPlan.v1`, `request_mn_object_existence_probe`, `mnObjectExistenceProbeFinished`, `release_acceptance.evaluate_acceptance(final_claim=True, verification_reports=[...])`. | Public preview is unaffected. Any final/v3 claim requires at least one current-document `PASS` `codex.mn.verificationReport.v1`. |

## Current Required Gates

| Gate | Required evidence | Current release decision |
| --- | --- | --- |
| Runtime shell | Current MN4 WebView reports the Knowledge OS anchors above. | Required for the preview package to be considered installed correctly. |
| Arbitrary-document UI functional acceptance | `ui_functional_acceptance.py` returns `codex-companion-ui-functional-acceptance-v1` with `webview_static_controls`, `webview_behavior_markers`, `webview_button_coverage`, `arbitrary_document_workspace`, `notebook_workspace_kernels`, `workspace_surface_actions`, and `native_scope_guards` PASS for a synthetic document payload. `webview_button_coverage` enumerates every static WebView button and fails if a button is not classified as actual browser-clicked, interaction-clicked, write-flow, panel-control, form/destructive, file-picker, close-only, or hidden-runtime; the current gate has no untested dynamic-runtime static button category. The settings-page `UI 功能验收` entry is exposed as `uiFunctionalAcceptanceButton` and calls `ui_functional_acceptance_summary`, which runs the same arbitrary-document gate from inside the plugin UI and renders `uiFunctionalAcceptanceLine` as `UI 功能验收：PASS / 11/11 / 阻塞 0` plus detailed check text. With `--browser-render`, it records `webview_browser_render` from a local headless browser and checks rendered workspace controls plus disabled no-scope native actions. With `--browser-interaction`, it records `webview_browser_interaction` by clicking Chat/Workspace mode, every visible Workspace Navigator surface, every Workbench tab, Command Pane toggle, settings context-scope auto selection, settings close, and history open/close through the browser DevTools protocol. With `--browser-actions`, it records `webview_browser_actions` by stubbing Companion, injecting an arbitrary `MNObject` plus notebook scope, and verifying `sendButton` chat submission, prompt clearing, `Enter key` submission, `conversation_new`, `conversationHistoryObjectButton`, `conversationHistoryAllButton`, context-scope switching, native `context` bridge refresh, `settings_update` for file paths, `update_check`, download-page `open_url`, `diagnose_permissions`, `open_full_disk_access_settings`, `request_pdf_cache`, `request_native_capability_probe`, Agent Plan, `request_mn_object_registry_scan`, `mn_read_tree`, `object_graph`, `object_activity`, `object_graph_relation_save`, `notebook_runbook_preflight_record`, Notebook Workspace, Object Browser, Operation Ledger, Verification Center, `verificationRepairPlanRecommendedButton`, `request_mn_object_existence_probe`, `knowledge_index_search`, `mindmap_target_status`, health, AI backend, logs, and history buttons send the expected backend/native actions without a connection-failure UI. The `buttonActionDeltas` evidence must show that each mapped button, including duplicate-action buttons such as refresh/filter and history scope switches, increased its own expected backend action count, and the same browser session must prove the settings UI displays `UI 功能验收：PASS`. With `--browser-write-actions`, it records `webview_browser_write_actions` by driving `generate_mindmap`, `draft_save`, `mindmap_diff_preview`, `request_mindmap_diff_apply`, `mindmapStudioVerifyButton`, `mindmapStudioRollbackButton`, `ai_edit_transaction_get`, `ai_edit_transaction_verify`, `request_mn_object_existence_probe`, `review_queue_add`, and native bridge calls `write_draft`, `accept_ai_edit_transaction`, `reject_ai_edit_transaction`. | Required for preview packaging and local development before claiming the Agent Workspace works for more than one hard-coded paper. |
| Single document acceptance | `Collect Single Document Acceptance.command`, `single_document_acceptance.py`, `single_document_acceptance_summary`, `realMnAcceptancePanel`, `realMnAcceptanceStatusLine`, `realMnAcceptanceChecklist`, `realMnAcceptanceRunAllButton`, `singleDocumentAcceptanceButton`, `mainUiFunctionalAcceptanceButton`, `realMnAcceptanceSafeEvidenceButton`, and the 主工作台 Verification Center `本文档验收` entry produce/consume same-topic evidence. The run-all button is a workflow entry: current-document acceptance first, arbitrary-document UI gate second, non-writing native evidence collection third. | Required before final v1.0. Public preview can ship while this remains documented as pending. |
| Native visible highlight | Same-topic native highlight event and `ZHIGHLIGHTS` blob evidence. | Still a hard blocker for final v1.0. |
| Signed/notarized pkg | Developer ID Installer signature, notarization, stapled ticket, Gatekeeper install assessment. | Hard blocker for final installer release. |
| Cross-machine install | Structured second-user/second-machine evidence with matching artifact hash and install doctor checks. | Hard blocker for final release. |
| final/v3 claim | `final_claim=True` release acceptance plus a current-document `PASS` `codex.mn.verificationReport.v1`. | Required before calling the product final v3. |

## One-Shot Goal Boundary

One-shot goals remain 一次性长任务. They do not save as persistent long-term context and 不会保存成长期当前目标. Ordinary chat, card generation, and mind-map generation must not inherit an old one-shot goal.

## Verification Commands

```bash
python3 -m unittest \
  tests.test_object_kernel \
  tests.test_source_registry \
  tests.test_external_gateway \
  tests.test_transaction_manager \
  tests.test_workflow_engine \
  tests.test_skill_marketplace \
  tests.test_verification_agent \
  tests.test_web_controls_static \
  tests.test_doctor_checks \
  tests.test_ui_functional_acceptance \
  tests.test_single_document_acceptance \
  tests.test_release_docs
```

```bash
node --check extension/codex.mn.assistant/main.js
node --check extension/codex.mn.assistant/web/app.js
git diff --check
```

## Current Blockers

Current `0.4.41` work is still a public preview, not final v1.0 or v3:

- Native visible highlight evidence is still required for final release.
- Same-topic single-document PASS evidence is still required.
- Signed and notarized pkg evidence is still required.
- Cross-machine install evidence is still required.
- Full v3 still needs a complete Notebook Knowledge IDE, cross-document Knowledge Graph Studio, visual Workflow Builder, stronger native object probe coverage, and a full Verification Studio with repair automation beyond the current first-stage Verification Center.
